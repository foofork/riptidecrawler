[target.x86_64-unknown-linux-gnu]
# Use the default system linker instead of lld
rustflags = ["-C", "link-arg=-fuse-ld=ld"]

[build]
# Ensure we use the correct target
target = "x86_64-unknown-linux-gnu"
