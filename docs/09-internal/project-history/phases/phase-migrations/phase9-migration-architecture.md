# Phase 9 Sprint 1: Architecture Diagrams

## Current State: CLI with Embedded Business Logic

```
┌─────────────────────────────────────────────────────────────────┐
│                      riptide-cli (5,200 LOC)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              CLI Commands (2,272 LOC)                     │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                           │  │
│  │  • job.rs (784 LOC)              ┌─────────────────────┐ │  │
│  │    - Submit, list, status        │   Uses but doesn't  │ │  │
│  │    - API client logic            │   extend existing   │ │  │
│  │    - HTTP requests               │   libraries!        │ │  │
│  │                                   └─────────────────────┘ │  │
│  │  • job_local.rs (636 LOC)                               │  │
│  │    - Local job manager                                  │  │
│  │    - File persistence                                   │  │
│  │    - State tracking                                     │  │
│  │                                   ┌─────────────────────┐ │  │
│  │  • cache.rs (262 LOC)            │   Duplicates logic  │ │  │
│  │    - Status, clear, warm         │   that should be in │ │  │
│  │    - Stats, validation           │   riptide-cache!    │ │  │
│  │                                   └─────────────────────┘ │  │
│  │  • browser_pool_manager.rs (456) ┌─────────────────────┐ │  │
│  │    - Pre-warming                 │   Dead code, never  │ │  │
│  │    - Health monitoring           │   actually used!    │ │  │
│  │    - Resource tracking           └─────────────────────┘ │  │
│  │                                                           │  │
│  │  • pdf_impl.rs (134 LOC)         ┌─────────────────────┐ │  │
│  │    - Load, parse, convert        │   Helpers that      │ │  │
│  │    - Page ranges                 │   belong in library │ │  │
│  │    - Output writing              └─────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         Other Commands (2,928 LOC)                        │  │
│  │  • extract.rs, render.rs, crawl.rs, etc.                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└───────────────────┬──────────────────────────────────────────────┘
                    │ Uses but doesn't extend properly
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Existing Library Crates                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  riptide-workers    riptide-cache    riptide-pool    riptide-pdf│
│  (Has job system)   (Has cache mgr)  (Has pool)      (Has ext.) │
│       ↑                   ↑               ↑               ↑      │
│       └───────────────────┴───────────────┴───────────────┘      │
│                   CLI bypasses these!                            │
└─────────────────────────────────────────────────────────────────┘
```

**Problems:**
1. **Duplication**: Job logic exists in both CLI and riptide-workers
2. **Tight Coupling**: CLI mixes UI concerns with business logic
3. **Poor Reusability**: Other tools can't use CLI's job/cache features
4. **Maintenance Burden**: Changes require updating multiple places

---

## Target State: Thin CLI with Rich Libraries

```
┌─────────────────────────────────────────────────────────────────┐
│                   riptide-cli (3,500 LOC)                       │
│                     -45% reduction!                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │       Thin CLI Adapters (500 LOC)                         │  │
│  │       Only formatting + user interaction                  │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │                                                           │  │
│  │  job.rs (<200 LOC)                                       │  │
│  │  ├─ JobApiClient adapter                                 │  │
│  │  ├─ LocalJobManager adapter                              │  │
│  │  └─ Format output (JSON/table/text)                      │  │
│  │                                                           │  │
│  │  cache.rs (<100 LOC)                                     │  │
│  │  ├─ CacheOperations adapter                              │  │
│  │  └─ Format stats/results                                 │  │
│  │                                                           │  │
│  │  browser.rs (<50 LOC)                                    │  │
│  │  ├─ PoolManager adapter                                  │  │
│  │  └─ Display pool stats                                   │  │
│  │                                                           │  │
│  │  pdf.rs (<30 LOC)                                        │  │
│  │  └─ Re-export library functions                          │  │
│  │                                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         Other Commands (2,928 LOC) - unchanged            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└───────────────────┬──────────────────────────────────────────────┘
                    │ Properly delegates to libraries
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Enhanced Library Crates                         │
│                  Now the single source of truth!                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐  ┌──────────────────┐                  │
│  │  riptide-workers   │  │  riptide-cache   │                  │
│  │  +850 LOC          │  │  +180 LOC        │                  │
│  ├────────────────────┤  ├──────────────────┤                  │
│  │ • JobApiClient     │  │ • CacheOperations│                  │
│  │ • LocalJobManager  │  │ • Result types   │                  │
│  │ • Storage          │  │ • Warm/Validate  │                  │
│  │ • Compatibility    │  │ • Stats          │                  │
│  └────────────────────┘  └──────────────────┘                  │
│                                                                  │
│  ┌────────────────────┐  ┌──────────────────┐                  │
│  │  riptide-pool      │  │  riptide-pdf     │                  │
│  │  +350 LOC          │  │  +80 LOC         │                  │
│  ├────────────────────┤  ├──────────────────┤                  │
│  │ • PoolManager      │  │ • load_pdf()     │                  │
│  │ • Health checks    │  │ • parse_pages()  │                  │
│  │ • Resource monitor │  │ • write_output() │                  │
│  └────────────────────┘  └──────────────────┘                  │
│                                                                  │
│  Benefits:                                                       │
│  ✓ Reusable by other tools (API server, workers, tests)        │
│  ✓ Single source of truth for business logic                   │
│  ✓ Easier to test in isolation                                 │
│  ✓ Clear separation of concerns                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## Migration Flow: Risk-Sorted Execution

```
Day 1: PDF Helpers (4 hours)
────────────────────────────────────────────────────────────
Risk: LOW ⚠️  | Test Updates: Minimal

  CLI pdf_impl.rs          riptide-pdf/utils.rs
  (134 LOC)
       │                          │
       │  1. Add functions        │
       │─────────────────────────>│
       │                          │
       │  2. Re-export            │
       │<─────────────────────────│
       │                          │
       └─> CLI becomes thin       │
           wrapper (<30 LOC)      │

Result: -104 LOC from CLI, +80 LOC to library

────────────────────────────────────────────────────────────
Day 2: Browser Pool (6 hours)
────────────────────────────────────────────────────────────
Risk: LOW ⚠️  | Test Updates: Moderate

  CLI browser_pool_mgr.rs  riptide-pool/manager.rs
  (456 LOC, dead code)
       │                          │
       │  1. Move manager         │
       │─────────────────────────>│
       │                          │
       │  2. Add health checks    │
       │─────────────────────────>│
       │                          │
       │  3. Create adapter       │
       │<─────────────────────────│
       │                          │
       └─> CLI adapter (<50 LOC)  │

Result: -406 LOC from CLI, +350 LOC to library

────────────────────────────────────────────────────────────
Day 3-4: Cache Operations (8 hours)
────────────────────────────────────────────────────────────
Risk: MEDIUM ⚡ | Test Updates: Significant

  CLI cache.rs             riptide-cache/operations.rs
  (262 LOC)
       │                          │
       │  1. Extract biz logic    │
       │─────────────────────────>│
       │                          │
       │  2. Define result types  │
       │─────────────────────────>│
       │                          │
       │  3. Update commands      │
       │<─────────────────────────│
       │                          │
       └─> CLI formatting only    │
           (<100 LOC)             │

Result: -162 LOC from CLI, +180 LOC to library

────────────────────────────────────────────────────────────
Day 5-6: Job Management (16 hours)
────────────────────────────────────────────────────────────
Risk: HIGH 🔥 | Test Updates: Extensive

  CLI job.rs + job_local.rs   riptide-workers/
  (1,420 LOC total)           api_client.rs + local_manager.rs
       │                             │
       │  Phase A: API client        │
       │────────────────────────────>│
       │                             │
       │  Phase B: Local manager     │
       │────────────────────────────>│
       │                             │
       │  Phase C: Compatibility     │
       │<────────────────────────────│
       │                             │
       └─> CLI adapters (<200 LOC)  │

Result: -1,220 LOC from CLI, +850 LOC to library

════════════════════════════════════════════════════════════
TOTAL IMPACT
════════════════════════════════════════════════════════════
CLI:       5,200 LOC → 3,428 LOC (-1,772 LOC, -34%)
Libraries: +1,460 LOC (net reusable code)
Reduction: 312 LOC net (after library additions)
```

---

## Dependency Graph: Before vs After

### Before Migration
```
┌─────────────┐
│ riptide-cli │ (5,200 LOC)
└──────┬──────┘
       │
       ├─────────────────────┐
       │                     │
       ▼                     ▼
┌────────────┐        ┌───────────┐
│ riptide-   │        │ riptide-  │
│ extraction │        │ browser   │
└────────────┘        └───────────┘
       │                     │
       │                     ▼
       │              ┌────────────┐
       │              │ riptide-   │
       │              │ pool       │
       │              └────────────┘
       │
       ├─────────────────────┐
       ▼                     ▼
┌────────────┐        ┌───────────┐
│ riptide-   │        │ riptide-  │
│ pdf        │        │ workers   │
└────────────┘        └───────────┘
                             │
                             ▼
                      ┌────────────┐
                      │ riptide-   │
                      │ cache      │
                      └────────────┘

Problem: CLI doesn't use riptide-workers, riptide-cache properly!
         Instead, it reimplements their functionality.
```

### After Migration
```
┌─────────────┐
│ riptide-cli │ (3,428 LOC) - Thin adapter layer
└──────┬──────┘
       │
       ├──────────────────────┬─────────────────┬────────────────┐
       │                      │                 │                │
       ▼                      ▼                 ▼                ▼
┌─────────────┐      ┌─────────────┐   ┌─────────────┐  ┌──────────┐
│ riptide-    │      │ riptide-    │   │ riptide-    │  │ riptide- │
│ workers     │      │ cache       │   │ pool        │  │ pdf      │
│             │      │             │   │             │  │          │
│ +850 LOC    │      │ +180 LOC    │   │ +350 LOC    │  │ +80 LOC  │
└──────┬──────┘      └──────┬──────┘   └──────┬──────┘  └────┬─────┘
       │                    │                 │               │
       └────────────────────┴─────────────────┴───────────────┘
                            │
                            ▼
                  ┌──────────────────┐
                  │  riptide-types   │ (Shared types)
                  └──────────────────┘

Benefits:
✓ Clear dependency hierarchy
✓ Libraries are independently testable
✓ CLI is just a thin presentation layer
✓ Other tools can reuse libraries directly
```

---

## Code Flow: Job Submission Example

### Before Migration (Current)
```
User runs: riptide job submit --url example.com

┌──────────────────────────────────────────────────────┐
│  CLI (job.rs) - 784 LOC                              │
├──────────────────────────────────────────────────────┤
│                                                       │
│  1. Parse command args (clap)                        │
│  2. Build HTTP request                               │
│  3. Send to API server                               │
│  4. Format response                                  │
│  5. Display to user                                  │
│                                                       │
│  Problem: HTTP client logic embedded in CLI!         │
│  Can't reuse this for tests or other tools.          │
└──────────────────────────────────────────────────────┘

User runs: riptide job-local submit --url example.com

┌──────────────────────────────────────────────────────┐
│  CLI (job_local.rs) - 636 LOC                        │
├──────────────────────────────────────────────────────┤
│                                                       │
│  1. Parse command args                               │
│  2. Create job ID                                    │
│  3. Write to filesystem                              │
│  4. Track in memory                                  │
│  5. Format response                                  │
│  6. Display to user                                  │
│                                                       │
│  Problem: Job management logic in CLI!               │
│  Separate implementation from riptide-workers!       │
└──────────────────────────────────────────────────────┘
```

### After Migration (Target)
```
User runs: riptide job submit --url example.com

┌──────────────────────────────────────────────────────┐
│  CLI Adapter (job.rs) - <100 LOC                     │
├──────────────────────────────────────────────────────┤
│  1. Parse command args                               │
│  2. Call JobApiClient::submit_job()      ───────┐    │
│  3. Format response                              │    │
│  4. Display to user                              │    │
└──────────────────────────────────────────────────┼────┘
                                                   │
                                                   ▼
               ┌────────────────────────────────────────┐
               │  riptide-workers (api_client.rs)       │
               ├────────────────────────────────────────┤
               │  JobApiClient::submit_job()            │
               │  • Build HTTP request                  │
               │  • Send to API server                  │
               │  • Parse response                      │
               │  • Return JobId                        │
               │                                        │
               │  Benefit: Reusable in tests,           │
               │           other tools, API server      │
               └────────────────────────────────────────┘

User runs: riptide job-local submit --url example.com

┌──────────────────────────────────────────────────────┐
│  CLI Adapter (job_local.rs) - <100 LOC              │
├──────────────────────────────────────────────────────┤
│  1. Parse command args                               │
│  2. Call LocalJobManager::submit_job()   ───────┐    │
│  3. Format response                              │    │
│  4. Display to user                              │    │
└──────────────────────────────────────────────────┼────┘
                                                   │
                                                   ▼
               ┌────────────────────────────────────────┐
               │  riptide-workers (local_manager.rs)    │
               ├────────────────────────────────────────┤
               │  LocalJobManager::submit_job()         │
               │  • Create job ID                       │
               │  • Write to filesystem                 │
               │  • Track in memory                     │
               │  • Return JobId                        │
               │                                        │
               │  Benefit: Same API as workers,         │
               │           testable independently       │
               └────────────────────────────────────────┘
```

---

## Test Architecture: Before vs After

### Before Migration
```
Tests scattered across CLI:

crates/riptide-cli/tests/
├── job_tests.rs              ❌ Tests API client (wrong layer)
├── cache_tests.rs            ❌ Tests cache ops (wrong layer)
├── pdf_tests.rs              ✓  Tests PDF commands (correct)
└── integration/
    └── cli_tests.rs          ✓  Tests CLI output (correct)

crates/riptide-cli/src/commands/
├── job.rs                    ❌ 0 unit tests
├── cache.rs                  ✓  5 inline tests
├── pdf_impl.rs               ✓  1 inline test
└── browser_pool_manager.rs   ✓  3 inline tests

Problem: Business logic tests live in CLI, not libraries!
```

### After Migration
```
Tests properly distributed:

Library Tests (Business Logic):
─────────────────────────────
crates/riptide-workers/tests/
├── api_client_tests.rs       ✓  Test HTTP requests
├── local_manager_tests.rs    ✓  Test job persistence
└── compat_tests.rs           ✓  Test migrations

crates/riptide-cache/tests/
├── operations_tests.rs       ✓  Test cache ops
└── warm_tests.rs             ✓  Test cache warming

crates/riptide-pool/tests/
├── manager_tests.rs          ✓  Test pool management
└── health_tests.rs           ✓  Test health checks

crates/riptide-pdf/tests/
├── utils_tests.rs            ✓  Test PDF helpers
└── parse_tests.rs            ✓  Test page parsing

CLI Tests (Presentation Layer):
────────────────────────────────
crates/riptide-cli/tests/
├── job_output_tests.rs       ✓  Test output formatting
├── cache_output_tests.rs     ✓  Test table/JSON format
└── integration/
    └── cli_tests.rs          ✓  Test end-to-end commands

Benefits:
✓ Clear test boundaries
✓ Faster library test execution
✓ Independent library testing
✓ Reduced CLI test complexity
```

---

## Performance Impact Analysis

### Memory Footprint
```
Before:
─────────────────────────────────────────────────
CLI binary size:     8.2 MB
Hot path LOC:        2,272 (business logic in CLI)
Instantiation cost:  50ms (CLI startup)

After:
─────────────────────────────────────────────────
CLI binary size:     7.8 MB (-5%)
Hot path LOC:        500 (thin adapters)
Instantiation cost:  48ms (-4%, library precompiled)

Library overhead:    +2ms per call (negligible)
```

### Build Times
```
Before:
─────────────────────────────────────────────────
cargo build -p riptide-cli     32s
cargo build --workspace        127s

After:
─────────────────────────────────────────────────
cargo build -p riptide-cli     28s (-12%)
cargo build -p riptide-workers 18s (new)
cargo build -p riptide-cache   12s (new)
cargo build -p riptide-pool    15s (new)
cargo build -p riptide-pdf     14s (new)
cargo build --workspace        145s (+18s total)

Parallel build:                98s (-23% wall time)

Benefits:
✓ CLI rebuilds faster (smaller crate)
✓ Libraries can be built in parallel
✓ Incremental builds more effective
```

### Runtime Performance
```
Command Execution:
─────────────────────────────────────────────────
                    Before    After    Difference
─────────────────────────────────────────────────
job submit          125ms     127ms    +2ms
job status          45ms      46ms     +1ms
cache status        78ms      79ms     +1ms
cache warm          2.3s      2.3s     0ms
pdf extract         890ms     891ms    +1ms

Library call overhead: <2ms per operation (acceptable)
```

---

## Migration Validation Checklist

### Code Quality Gates
```
✓ All migrations pass:
  ├─ cargo build --workspace
  ├─ cargo test --workspace
  ├─ cargo clippy --workspace -- -D warnings
  └─ cargo fmt -- --check

✓ Test coverage maintained:
  ├─ riptide-workers: >80% coverage
  ├─ riptide-cache: >80% coverage
  ├─ riptide-pool: >80% coverage
  └─ riptide-pdf: >80% coverage

✓ Documentation complete:
  ├─ cargo doc --workspace --no-deps
  ├─ All public APIs documented
  └─ Usage examples provided

✓ Performance stable:
  ├─ cargo bench (no regressions)
  ├─ CLI startup time <60ms
  └─ Library call overhead <5ms
```

### User Experience Validation
```
✓ CLI behavior unchanged:
  ├─ Output format identical (JSON/table/text)
  ├─ Error messages preserved
  ├─ Exit codes consistent
  └─ Interactive features work (watch mode)

✓ Backwards compatibility:
  ├─ Job storage format compatible
  ├─ Cache format compatible
  ├─ Config files load correctly
  └─ Migration path documented

✓ Documentation updated:
  ├─ CLI help text accurate
  ├─ Migration guide complete
  ├─ Breaking changes documented
  └─ Examples work
```

---

**Document Version**: 1.0
**Created**: 2025-10-23
**Author**: System Architect
**Status**: Ready for Review
