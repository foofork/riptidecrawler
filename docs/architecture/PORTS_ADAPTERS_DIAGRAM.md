# Riptide Hexagonal Architecture Diagram

**Companion to:** PORTS_ADAPTERS_MAPPING.md
**Purpose:** Visual representation of ports and adapters architecture

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        PRIMARY ADAPTERS (Driving)                        │
│                                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │
│  │   HTTP API   │  │     CLI      │  │  WebSocket   │                  │
│  │  (actix-web) │  │    (clap)    │  │     API      │                  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘                  │
│         │                  │                  │                          │
└─────────┼──────────────────┼──────────────────┼──────────────────────────┘
          │                  │                  │
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼──────────────────────────┐
│                     APPLICATION LAYER (Facades)                           │
│                     riptide-facade/src/facades/                           │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  BrowserFacade   ScraperFacade   ExtractionFacade               │   │
│  │  CrawlFacade     PipelineFacade  SearchFacade                   │   │
│  │  SessionFacade   StreamingFacade PdfFacade                      │   │
│  │  RenderFacade    LlmFacade       TraceFacade                    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Workflow Orchestration:                                         │   │
│  │  • Transaction coordination                                      │   │
│  │  • Authorization policies                                        │   │
│  │  • Idempotency management                                        │   │
│  │  • Event emission                                                │   │
│  │  • Retry/timeout coordination                                    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
└───────────────────────────────┬───────────────────────────────────────────┘
                                │ depends on (via Arc<dyn Port>)
                                │
┌───────────────────────────────▼───────────────────────────────────────────┐
│                         DOMAIN LAYER (Ports)                               │
│                      riptide-types/src/ports/                             │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  OUTBOUND PORTS (Driven Side - Infrastructure Capabilities)     │    │
│  │                                                                  │    │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │    │
│  │  │  Persistence   │  │  Features      │  │  Infrastructure│   │    │
│  │  ├────────────────┤  ├────────────────┤  ├────────────────┤   │    │
│  │  │ Repository<T>  │  │ BrowserDriver  │  │ HttpClient     │   │    │
│  │  │ Transaction    │  │ PdfProcessor   │  │ CacheStorage   │   │    │
│  │  │ EventBus       │  │ SearchEngine   │  │ Clock          │   │    │
│  │  │ SessionStorage │  │                │  │ Entropy        │   │    │
│  │  │ Idempotency    │  │                │  │ Pool<T>        │   │    │
│  │  │                │  │                │  │ RateLimiter    │   │    │
│  │  └────────────────┘  └────────────────┘  └────────────────┘   │    │
│  │                                                                  │    │
│  │  ┌────────────────┐  ┌────────────────┐                        │    │
│  │  │  Streaming     │  │  Monitoring    │                        │    │
│  │  ├────────────────┤  ├────────────────┤                        │    │
│  │  │ StreamTransport│  │ MetricsCollector│                       │    │
│  │  │ StreamProcessor│  │ HealthCheck    │                        │    │
│  │  │ StreamLifecycle│  │ HealthRegistry │                        │    │
│  │  └────────────────┘  └────────────────┘                        │    │
│  │                                                                  │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                           │
│  Pure trait definitions - NO implementation details                      │
│  NO external dependencies (except std, async-trait, serde)               │
│                                                                           │
└───────────────────────────────┬───────────────────────────────────────────┘
                                │ implemented by
                                │
┌───────────────────────────────▼───────────────────────────────────────────┐
│                   INFRASTRUCTURE LAYER (Adapters)                         │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  PostgreSQL Adapters (riptide-persistence/src/adapters/)        │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  PostgresRepository<T>      ✅ impl Repository<T>          │ │   │
│  │  │  PostgresTransactionManager ✅ impl TransactionManager      │ │   │
│  │  │  PostgresTransaction        ✅ impl Transaction            │ │   │
│  │  │  PostgresSessionStorage     ✅ impl SessionStorage         │ │   │
│  │  │  OutboxEventBus             ✅ impl EventBus               │ │   │
│  │  │  PrometheusMetrics          ✅ impl MetricsCollector       │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Redis Adapters (riptide-cache/src/adapters/)                   │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  RedisIdempotencyStore      ✅ impl IdempotencyStore       │ │   │
│  │  │  RedisSessionStorage        ✅ impl SessionStorage         │ │   │
│  │  │  RedisRateLimiter           ✅ impl RateLimiter            │ │   │
│  │  │  RedisPerHostRateLimiter    ✅ impl PerHostRateLimiter     │ │   │
│  │  │  RedisCache                 ❌ MISSING (impl CacheStorage) │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  HTTP Adapter (riptide-fetch/src/adapters/)                     │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  ReqwestHttpClient          ✅ impl HttpClient             │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Browser Adapters (riptide-browser/)                            │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  🔴 PROBLEM: Abstraction mismatch                          │ │   │
│  │  │                                                             │ │   │
│  │  │  Domain Port:        riptide-types::ports::BrowserDriver   │ │   │
│  │  │  Infrastructure:     riptide-browser::BrowserEngine ≠      │ │   │
│  │  │                                                             │ │   │
│  │  │  ChromiumoxideEngine        ✅ impl BrowserEngine          │ │   │
│  │  │  SpiderChromeEngine         ✅ impl BrowserEngine          │ │   │
│  │  │                                                             │ │   │
│  │  │  ❌ MISSING: BrowserDriverAdapter (BrowserEngine →         │ │   │
│  │  │              BrowserDriver)                                │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  PDF Adapters (riptide-pdf/)                                    │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  🔴 PROBLEM: Trait name collision                          │ │   │
│  │  │                                                             │ │   │
│  │  │  Domain Port:        riptide-types::ports::PdfProcessor    │ │   │
│  │  │  Infrastructure:     riptide-pdf::PdfProcessor (different!)│ │   │
│  │  │                                                             │ │   │
│  │  │  PdfiumProcessor            ✅ impl riptide-pdf::PdfProc   │ │   │
│  │  │  DefaultPdfProcessor        ✅ impl riptide-pdf::PdfProc   │ │   │
│  │  │                                                             │ │   │
│  │  │  ❌ MISSING: PdfProcessorAdapter (internal trait →         │ │   │
│  │  │              domain port)                                  │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Search Engine Adapters (riptide-search/)                       │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  ⚠️  NEEDS INVESTIGATION                                    │ │   │
│  │  │                                                             │ │   │
│  │  │  Domain Port:        riptide-types::ports::SearchEngine    │ │   │
│  │  │  Infrastructure:     ??? (not analyzed yet)                │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Streaming Adapters (riptide-api/src/adapters/)                 │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  WebSocketTransport         ✅ impl StreamingTransport     │ │   │
│  │  │  SseTransport               ✅ impl StreamingTransport     │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Test Doubles (riptide-types/src/ports/ & composition/stubs.rs) │   │
│  │  ┌────────────────────────────────────────────────────────────┐ │   │
│  │  │  InMemoryCache              ✅ impl CacheStorage           │ │   │
│  │  │  FakeClock                  ✅ impl Clock                  │ │   │
│  │  │  DeterministicEntropy       ✅ impl Entropy                │ │   │
│  │  │  InMemoryRepository<T>      ✅ impl Repository<T>          │ │   │
│  │  │  InMemoryEventBus           ✅ impl EventBus               │ │   │
│  │  │  InMemoryIdempotencyStore   ✅ impl IdempotencyStore       │ │   │
│  │  │  InMemoryTransactionManager ✅ impl TransactionManager     │ │   │
│  │  └────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

## Dependency Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                     DEPENDENCY DIRECTION                          │
└──────────────────────────────────────────────────────────────────┘

HTTP API                    CLI
   │                         │
   ├─────────────┬───────────┤
   │             │           │
   ▼             ▼           ▼
┌──────────────────────────────────┐
│   ApplicationContext (DI Root)    │
│                                   │
│   ✅ clock: Arc<dyn Clock>        │
│   ✅ entropy: Arc<dyn Entropy>    │
│   ✅ tx_manager: Arc<dyn TxMgr>   │
│   ✅ user_repo: Arc<dyn Repo<T>>  │
│   ✅ event_bus: Arc<dyn EventBus> │
│   ✅ idempotency: Arc<dyn IdStore>│
│                                   │
│   ❌ browser_driver: MISSING      │
│   ❌ pdf_processor: MISSING       │
│   ❌ search_engine: MISSING       │
│   ❌ cache_storage: MISSING       │
│   ❌ http_client: MISSING         │
└────────────┬──────────────────────┘
             │ injects ports into
             ▼
┌──────────────────────────────────┐
│       Facade Layer                │
│                                   │
│  Facades receive Arc<dyn Port>    │
│  via constructor injection        │
│                                   │
│  Example:                         │
│  ExtractionFacade::new(           │
│      browser: Arc<dyn BrowserDrv>,│
│      cache: Arc<dyn CacheStorage>,│
│      events: Arc<dyn EventBus>    │
│  )                                │
└────────────┬──────────────────────┘
             │ uses ports (traits)
             ▼
┌──────────────────────────────────┐
│       Port Traits (Interfaces)    │
│                                   │
│  Pure trait definitions           │
│  NO concrete types                │
│  NO infrastructure knowledge      │
└────────────▲──────────────────────┘
             │ implemented by
             │
┌────────────┴──────────────────────┐
│    Infrastructure Adapters        │
│                                   │
│  Concrete implementations         │
│  External dependencies OK         │
│  Technology-specific code         │
└───────────────────────────────────┘
```

## Port-to-Adapter Mapping Matrix

| Port Trait | Production Adapter | Test Adapter | Status |
|------------|-------------------|--------------|--------|
| `Repository<T>` | `PostgresRepository<T>` | `InMemoryRepository<T>` | ✅ |
| `TransactionManager` | `PostgresTransactionManager` | `InMemoryTransactionManager` | ✅ |
| `Transaction` | `PostgresTransaction` | `InMemoryTransaction` | ✅ |
| `EventBus` | `OutboxEventBus` | `InMemoryEventBus` | ✅ |
| `SessionStorage` | `PostgresSessionStorage`<br>`RedisSessionStorage` | `InMemorySessionStorage` | ✅ |
| `IdempotencyStore` | `RedisIdempotencyStore` | `InMemoryIdempotencyStore` | ✅ |
| `HttpClient` | `ReqwestHttpClient` | `MockHttpClient` | ✅ |
| `RateLimiter` | `RedisRateLimiter` | N/A | ✅ |
| `PerHostRateLimiter` | `RedisPerHostRateLimiter` | N/A | ✅ |
| `StreamingTransport` | `WebSocketTransport`<br>`SseTransport` | N/A | ✅ |
| `MetricsCollector` | `PrometheusMetrics` | N/A | ✅ |
| `Clock` | `SystemClock` | `FakeClock` | ✅ |
| `Entropy` | `SystemEntropy` | `DeterministicEntropy` | ✅ |
| `Pool<T>` | `NativePool<T>`<br>`BrowserPool` | N/A | ✅ |
| **`BrowserDriver`** | **❌ MISSING** | N/A | 🔴 |
| **`PdfProcessor`** | **❌ MISSING** | N/A | 🔴 |
| **`SearchEngine`** | **❌ MISSING** | N/A | 🔴 |
| **`CacheStorage`** | **❌ MISSING** (Redis) | `InMemoryCache` | ⚠️ |

## Critical Architecture Violations

### 🔴 Violation 1: Browser Abstraction Mismatch

```
┌─────────────────────────────────────────────────────────────┐
│  Domain Port (riptide-types/src/ports/features.rs)          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  pub trait BrowserDriver {                          │   │
│  │      async fn navigate(&self, url: &str) -> ...;    │   │
│  │      async fn execute_script(&self, ...) -> ...;    │   │
│  │  }                                                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                    ❌ NOT IMPLEMENTED BY ❌
┌─────────────────────────────────────────────────────────────┐
│  Infrastructure (riptide-browser/src/abstraction/)           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  pub trait BrowserEngine {  // ← Different trait!   │   │
│  │      async fn navigate(&self, params: ...) -> ...;  │   │
│  │  }                                                   │   │
│  │                                                      │   │
│  │  impl BrowserEngine for ChromiumoxideEngine { }     │   │
│  │  impl BrowserEngine for SpiderChromeEngine { }      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

SOLUTION: Create adapter layer
┌─────────────────────────────────────────────────────────────┐
│  riptide-browser/src/adapters/browser_driver_adapter.rs     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  pub struct BrowserDriverAdapter {                  │   │
│  │      engine: Arc<dyn BrowserEngine>,                │   │
│  │  }                                                   │   │
│  │                                                      │   │
│  │  impl BrowserDriver for BrowserDriverAdapter {      │   │
│  │      async fn navigate(&self, url: &str) -> ... {   │   │
│  │          let params = NavigateParams { url, ... };  │   │
│  │          self.engine.navigate(params).await?;       │   │
│  │      }                                               │   │
│  │  }                                                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 🔴 Violation 2: PDF Processor Trait Collision

```
┌─────────────────────────────────────────────────────────────┐
│  Domain Port (riptide-types/src/ports/features.rs)          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  pub trait PdfProcessor {                           │   │
│  │      async fn extract_text(&self, ...) -> ...;      │   │
│  │  }                                                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                    ❌ DIFFERENT TRAIT ❌
┌─────────────────────────────────────────────────────────────┐
│  Infrastructure (riptide-pdf/src/processor.rs)               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  pub trait PdfProcessor {  // ← Same name, diff API│   │
│  │      async fn process_pdf(&self, ...) -> ...;       │   │
│  │  }                                                   │   │
│  │                                                      │   │
│  │  impl PdfProcessor for PdfiumProcessor { }          │   │
│  │  impl PdfProcessor for DefaultPdfProcessor { }      │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

SOLUTION: Create adapter or rename infrastructure trait
```

### ⚠️ Warning: ApplicationContext Incomplete

```rust
// Current state
pub struct ApplicationContext {
    pub clock: Arc<dyn Clock>,           // ✅ Present
    pub entropy: Arc<dyn Entropy>,       // ✅ Present
    pub event_bus: Arc<dyn EventBus>,    // ✅ Present

    // ❌ Missing feature ports:
    // pub browser_driver: Arc<dyn BrowserDriver>,
    // pub pdf_processor: Arc<dyn PdfProcessor>,
    // pub search_engine: Arc<dyn SearchEngine>,
    // pub cache_storage: Arc<dyn CacheStorage>,
    // pub http_client: Arc<dyn HttpClient>,
}

// Facades cannot receive these via DI!
```

## Remediation Roadmap

### Phase 1: Critical Adapters (Week 1)
1. Create `BrowserDriverAdapter` in `riptide-browser/src/adapters/`
2. Create `PdfProcessorAdapter` in `riptide-pdf/src/adapters/`
3. Add ports to `ApplicationContext`

### Phase 2: Complete Abstractions (Week 2)
4. Create `RedisCache` implementing `CacheStorage`
5. Investigate and fix `SearchEngine` implementation
6. Update all facades to use DI ports

### Phase 3: Testing & Documentation (Week 3)
7. Audit all facade dependencies
8. Create architecture tests enforcing boundaries
9. Update ADRs and C4 diagrams

---

**Legend:**
- ✅ Complete and correct implementation
- ⚠️ Partial implementation, needs completion
- ❌ Missing implementation
- 🔴 Architecture violation requiring immediate attention
